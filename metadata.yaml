name: astrbot_plugin_echo # 插件唯一识别名，以 astrbot_plugin_ 前缀开头
display_name: echo # 用于展示的名字，可以是方便人类阅读的名字（需要版本 >= v4.5.0，低版本不会报错，请放心填写）
desc: 让bot原样返回你发送的消息，用于快速验证AstrBot插件系统的最小可运行链路 # 插件简短描述
version: v1.0.0 # 插件版本号。格式：v1.1.1 或者 v1.1
author: ocetars # 作者
repo: https://github.com/ocetars/astrbot_plugin_echo # 插件的仓库地址
