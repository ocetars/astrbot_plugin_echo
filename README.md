# Echo 插件

最简化的 AstrBot echo 插件，用于快速验证AstrBot的消息链路。

A minimal AstrBot echo plugin for testing the AstrBot message chain.

## 功能描述

让机器人复读你发送的文字消息。

## 使用方法

发送 `/echo` 命令，后面跟随要复读的文字内容：

- `/echo 你好` - 机器人会回复 "你好"
- `/echo` - 机器人会回复 "PONG"
